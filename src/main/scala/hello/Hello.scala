package hello

object Hello {
  def longer(text: String): String =
    text + "abit more"
  def main(args: Array[String]) : Unit =
  {
      var y = 100

      val bigger = y. + (4)

      println(s"Result ${longer("Hihi")}")
  }
}
